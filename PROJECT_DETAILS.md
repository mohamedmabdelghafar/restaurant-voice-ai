# توثيق مشروع SmartJump.AI - نظام الطلب الصوتي للمطاعم

## 1. نظرة عامة على المشروع (Project Overview)

**SmartJump.AI** هو نظام متطور للطلب الصوتي يعتمد على الذكاء الاصطناعي (AI Voice Ordering System)، مصمم خصيصاً للمطاعم. يقوم النظام بدمج تقنيات الاتصال الصوتي الآلي (Retell AI) مع أنظمة نقاط البيع (Square POS) لتوفير تجربة طلب سلسة وتلقائية بالكامل عبر الهاتف.

**الهدف الرئيسي:** أتمتة عملية استقبال طلبات المطاعم عبر الهاتف، وتحويل المكالمات الصوتية إلى طلبات رقمية مسجلة مباشرة في نظام الكاشير (POS).

---

## 2. هيكلة وتنظيم المشروع (Project Structure)

المشروع مبني باستخدام **Node.js** وتنظيمه يعتمد على فصل المسؤوليات (Separation of Concerns) لضمان سهولة الصيانة والتطوير.

### المجلدات الرئيسية

- **`src/api/`**: يحتوي على نقاط الاتصال (Endpoints) الخاصة بالواجهة البرمجية (مثل القائمة، الطلبات، المصادقة).
- **`src/auth/`**: المسؤول عن عمليات التحقق والأمان (OAuth لـ Square، JWT للمستخدمين).
- **`src/database/`**: ملفات قاعدة البيانات (Schema، الاتصال، الإعداد).
- **`src/webhooks/`**: استقبال الإشعارات الفورية من Square و Retell AI.
- **`src/n8n/`**: ملفات تكامل الأتمتة (Workflows) مع نظام n8n.
- **`src/middleware/`**: برمجيات وسيطة للأمان والضغط (Compression, Security).
- **`src/utils/`**: أدوات مساعدة (التشفير، التسجيل - Logger).

---

## 3. آلية العمل (How it Works)

يعمل النظام كحلقة وصل ذكية بين العميل والمطعم:

1. **اتصال العميل**: يتصل العميل برقم المطعم، فيرد عليه المجيب الآلي الذكي (Retell AI).
2. **معالجة الصوت**: يفهم الذكاء الاصطناعي طلب العميل (باستخدام LLMs).
3. **إدارة القائمة**: يتحقق النظام من توفر الأصناف وسعرها من خلال الربط المباشر مع Square POS.
4. **إنشاء الطلب**: عند تأكيد العميل، يتم إرسال الطلب تلقائياً إلى نظام Square في المطعم كأنه طلب عادي.
5. **التزامن**: يتم تحديث حالة الطلب وإرسال إشعارات فورية عبر الـ Webhooks.

---

## 4. كيفية الإنشاء والإعداد (Installation & Setup)

### المتطلبات المسبقة

- **Node.js** (الإصدار 18 أو أحدث).
- **حساب Square Developer** (للحصول على بيانات الربط).
- **حساب Retell AI** (لخدمة الصوت).
- **قاعدة بيانات** (PostgreSQL أو SQLite).

### خطوات التثبيت

1. **تثبيت الحزم البرمجية:**

    ```bash
    npm install
    ```

2. **إعداد ملف البيئة:**
    - قم بنسخ ملف `.env.example` إلى `.env`.
    - املأ البيانات المطلوبة (Square Access Token, App ID, Retell API Key, Database URL).

3. **تجهيز قاعدة البيانات:**

    ```bash
    npm run db:setup
    ```

4. **تشغيل الخادم:**

    ```bash
    npm start         # للتشغيل العادي
    npm run dev       # لوضع التطوير
    ```

---

## 5. أهم المميزات (Key Features)

- **🎤 طلب صوتي بالذكاء الاصطناعي**: محادثة طبيعية مع العميل دون تدخل بشري.
- **🏪 تكامل كامل مع Square POS**: مزامنة القائمة، المخزون، والطلبات في الوقت الفعلي.
- **🛡️ أمان عالٍ (Enterprise-grade Security)**:
  - حماية من هجمات XSS و SQL Injection.
  - تحديد عدد الطلبات (Rate Limiting) لمنع الهجمات.
  - تشفير البيانات الحساسة.
- **🔄 أتمتة متقدمة (n8n)**: إمكانية ربط النظام بسير عمل معقد ومخصص.
- **📊 واجهة برمجية موحدة (Unified API)**: سهولة الربط مع تطبيقات أخرى.

---

## 6. نصائح للعمل بكفاءة أشد (Tips for Efficiency)

### 🚀 تطوير الأداء

- **استخدم الـ Caching**: لتقليل الاستعلامات المتكررة لقائمة الطعام من Square.
- **تفعيل الضغط (Gzip)**: تم تفعيله بالفعل في `middleware/compression.js` لتسريع الاستجابة.

### 🛡️ الحفاظ على الأمان

- **تحديث المفاتيح دورياً**: قم بتغيير الـ API Keys و Secrets بشكل دوري.
- **مراقبة السجلات (Logs)**: راقب ملفات الـ Logs من خلال `src/utils/logger.js` لاكتشاف أي أخطاء أو محاولات اختراق مبكراً.

### 🛠️ الصيانة والتوسع

- **التزم بالهيكلية**: عند إضافة ميزة جديدة، ضع الملف في المجلد المناسب (`api` للمسارات، `utils` للأدوات) ولا تضع كل الكود في `server.js`.
- **استخدم الـ Webhooks بذكاء**: اعتمد على الـ Webhooks لتحديث حالة الطلبات بدلاً من الاستعلام المستمر (Polling).

---
**تم إعداد هذا التوثيق لضمان فهم شامل للنظام والمساعدة في إدارته وتطويره بأعلى كفاءة.**
